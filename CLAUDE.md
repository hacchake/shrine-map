# 神社まつりマップ (shrine-map)

日本全国の神社と例祭日を地図表示するプロジェクト。GitHub Pagesで公開: https://hacchake.github.io/shrine-map

## 構成

- `index.html` — Leaflet.jsベースの地図UI（GitHub Pagesで配信）
- `data.json` — 神社データ本体（5万件超）。**巨大なので全読み込み禁止**。検索はpythonかjq/grepで
- `scrape_*.py` — 都道府県別スクレイパー。出力は `{県名}_raw.json`
- `merge_new_prefectures.py` — raw jsonをdata.jsonにマージ（同一sourceは削除して再追加）
- `geocode_file.py` — 座標なしレコードをGSI APIで補完する汎用ツール
- `NOTES_*.md` — セッションごとの調査メモ・引き継ぎ

## データスキーマ（data.json / raw json共通）

```json
{"id": "...", "name": "疫神社", "pref": "岡山県", "address": "...", "deity": "素盞嗚尊",
 "lat": 34.6, "lng": 133.9,
 "festivals": [{"month": 10, "date_str": "10月第2日曜日", "name": "秋祭"}],
 "festivals_raw": "...", "notes": "", "official_url": "", "source_url": "...", "source": "okayama_jinjacho"}
```

- festivalsのmonthはint必須（地図のフィルタで使用）
- addressは「○○県」から始める（県判定は `startswith('○○県')`。「岩手」だけだと岩手郡に誤マッチした前科あり）

## 典型的なワークフロー

```bash
python3 scrape_XXX.py                 # → XXX_raw.json
python3 merge_new_prefectures.py XXX_raw.json --dry-run   # 件数確認
python3 merge_new_prefectures.py XXX_raw.json             # 本マージ
git add -A && git commit && git push  # Pagesに自動反映
```

## スクレイパー作成時の注意

- ラベル完全一致は禁物。実サイトは「住 所」「例 祭」のように空白が混入する（青森で実例）
- 正規表現の `\s*` は改行を越えて次行を拾う。行内空白は `[ \t\u3000]*` を使う（香川で実例）
- 座標を地図リンクから取るときは `soup.get_text()` でなく生HTML（`r.text`）を検索（href属性はget_textに含まれない）
- ジオコーディングはGSI API（`msearch.gsi.go.jp/address-search/AddressSearch?q=`）、sleep 0.1秒
- 各リクエスト間 sleep 1秒、User-Agentに連絡目的を明記
- robots.txtで拒否されているサイト（例: 北海道神社庁）はスクレイプしない
- 由緒などの長文は著作権配慮で取得しない。事実データ（名称/住所/祭神/例祭日/座標）のみ

## 未完了タスク（2026-07-04深夜時点）

1. ~~岩手・山形・青森・香川スクレイパーの実サイト通し実行~~ → 完了
2. ~~okinawa_raw.json のジオコーディング＋マージ~~ → 完了（16件全件成功）
3. ~~奈良の例祭パース率改善~~ → 調査済み。サイト側の「年中行事」欄自体が97%空で、スクレイパー改善では解決不可と判断（対応不要）
4. UI改善: 詳細モーダル（実装済み、ユーザーのブラウザ確認待ち・未コミット）、
   カタカナ表示修正（下記13で正体判明・部分対応済み）
5. ~~ジオコーディング全体強化~~ → 完了（3,408件中3,348件=98.2%を補完）。残り60件（addressありでも失敗、静岡5件など）は個別調査が必要
6. ~~青森: 詳細ページを持たない神社の追加抽出~~ → 完了（98件→732件）
7. ~~静岡データの文字化け調査~~ → 完了。cp932/UTF-8二重デコードバグと判明、下記13参照
8. data.jsonのjson整形をmerge_new_prefectures.pyとgeocode_file.pyで統一（indent設定が食い違いコミット差分が肥大化）
9. ~~富山県神社庁データ追加~~ → 完了（scrape_toyama.py新規作成、3,188件。サイトに御祭神フィールド自体が無いためdeityは常に空）
10. ~~福井県神社庁データ追加~~ → 完了（scrape_fukui.py新規作成、1,695件、御祭神率99.8%）
11. ~~群馬の例祭率0%原因調査~~ → 完了。サイト側に例祭フィールド自体が無いのが原因（バグではない）。scrape_gunma.py新規作成し再取得
12. ~~佐賀・熊本・鹿児島の神社庁データ追加~~ → 完了（佐賀132件・例祭53.8%、熊本571件・名称+住所のみ、鹿児島1,104件・例祭99.8%）
13. ~~静岡・長野・岐阜・神奈川のcp932/UTF-8二重デコード文字化け~~ → 静岡・長野は完了。
    静岡: scrape_shizuoka.py新規作成・全1,354件再取得（文字化け0件）。
    長野: scrape_nagano.pyのエンコーディング判定を修正・全2,093件再取得（文字化け実質0件、
    reisai→festivals変換も追加し例祭率0%→14.9%に改善）。
    岐阜(12件)・神奈川(1件)はスクレイパー自体が存在せず影響も小さいため未対応（NOTES_20260704.md参照）
14. 祭典(festivals)データの品質問題を追加調査（UI改善セッション中にユーザー指摘で発覚）:
    - 佐賀: 自作scrape_saga.pyのバグ（改行区切り「名前 日付（通称）」形式で名前/日付を取り違え）
      → 修正・再取得・push済み
    - **愛媛(ehime_jinjacho) 849件中825件**でfestivalsのname/date_strに同一文字列が完全重複格納
      → **スクレイパーが存在せず新規作成が必要**（優先度: 高、影響範囲が広い）
    - **岡山(okayama_jinjacho) 1,606件**で複数祭典を持つ神社の名前/日付がズレて合体
      → **スクレイパーが存在せず新規作成が必要**
    - 神奈川37件・ホトカミ11件も祭典の分離不備あり。スクレイパー不在のため未対応
    詳細はNOTES_20260704.md参照。愛媛・岡山は静岡と同程度の新規作成作業が必要、ユーザー優先度判断待ち
15. UI: カード/ポップアップ/モーダルの代表祭典表示を「今日から最も近い開催日」順にソートするよう
    index.html修正 → 完了（旧: 単なる掲載順で先頭を表示していた）
13. 長崎・大分・宮崎・高知のデータ追加（未着手・優先度低）: 長崎はサイト消失（ドメイン名前解決不能）、大分・宮崎は神社庁公式サイト自体が存在しない、高知はWixサイトでJSレンダリングのため現状のツールでは取得不可（ヘッドレスブラウザが使えるようになれば再検討）

詳細は NOTES_20260703.md・NOTES_20260704.md・SUMMARY.md を参照。data.json合計: 63,486件（2026-07-04深夜時点）
