# 調査メモ 2026-07-04（群馬0%調査、九州6県+高知 神社庁サイト検証）

## ① 群馬 例祭率0%の原因調査

**結論: パースの不具合ではない。仕様上の制約。**

群馬県神社庁サイト（https://www.gunma-jinjacho.jp/jinja/）の「県内神社紹介」ページを
実確認したところ、支部別テーブルには「神社名／カナ／所在地／連絡先／ホームページ」の
5列のみで、例祭・御祭神の項目自体が存在しない。香川・熊本と同型のケース。

旧 `scrape_gunma.py` はリポジトリから失われていた（`gunma_raw.json`はコミット済だが
生成スクリプトが無い状態）ため、新規に `scrape_gunma.py` を作成して再取得した。
再取得により official_url（32件）とnotes（支部名）を新たに取得できた（1184→1185件）。

なお調査序盤、`data.json`のgunma_jinjacho名称が文字化けしているように見えて
「エンコーディング破損」を疑ったが、これは**Windows端末のコンソールがcp932で
UTF-8文字列を表示しようとした際の表示上のバグ**であり、実際のファイル内容は
正しいUTF-8だった（バイト列を直接ダンプして確認）。今後同様の調査をする際は
`python3 script.py` の標準出力を直接見るのではなく、ファイルに書き出してから
`Read`ツールで読むか `PYTHONIOENCODING=utf-8` を設定すること。素の文字化け表示に
惑わされて「データ破損」と誤診断しないよう注意。

## ② 九州6県+高知 神社庁サイト調査

| 県 | サイト | 判定 | 例祭 | 備考 |
|---|---|---|---|---|
| 佐賀 | saga-jinjacho.jp | ◎ スクレイパー作成・実行・マージ済 | あり(53.8%) | WordPress、WP REST API(`/wp-json/wp/v2/pages`)で全151ページ取得。本文の【祭礼】ラベルをパース |
| 熊本 | kumamotokenjinjacho.jp | ◎ スクレイパー作成・実行中 | なし | WP独自投稿タイプ`patron-god`（氏神様を探す）。571件だが名称+住所のみ（例祭・御祭神・座標は掲載なし）→香川方式 |
| 鹿児島 | kagojinjacho.or.jp | ◎ スクレイパー作成・実行中 | あり(99.8%見込み) | WordPress通常post。`/wp-json/wp/v2/posts`で全1250件取得可、本文に「神社名：」を含む1104件が神社データ。例祭日は漢数字表記（例:九月二十三日） |
| 長崎 | nagasaki-jinjacho.or.jp | × スキップ | - | ドメインが名前解決不能（NXDOMAIN）。サイト自体が消失した模様。将来的に別ドメインへの移転がないか再調査が必要 |
| 大分 | (公式サイトなし) | × スキップ | - | 神社庁公式サイトが存在しない（電話のみ対応）ことをWeb検索で確認 |
| 宮崎 | (公式サイトなし) | × スキップ | - | 神社庁公式サイトが存在しない。「宮巡」(m-shinsei.jp)は神職有志の非公式サイトのため対象外 |
| 高知 | kochi-jinjyacho.com | × スキップ（技術的制約） | - | Wixサイト。神社一覧はJSでレンダリングされるテーブル/リピーター（Velo Cloud Data API）で、静的HTML取得では中身が空。個別ページやsitemapにも神社データなし。ヘッドレスブラウザが無いと事実上スクレイプ不可能 |

### 佐賀（scrape_saga.py 新規）
- WP REST APIで151ページ全件取得（クロール不要）
- 本文は自由記述、ラベルは全角【】。鎮座地/祭神(or御祭神)/祭礼(まれに例祭)
- 由緒・神事と芸能・社宝は著作権配慮で取得しない
- 祭礼欄は「名前（日付）」の羅列だが括弧なしの場合もある（例: 荒穂神社）→
  括弧パターンを先に抽出し、残りは日付パターン(N月N日)を手がかりに分割するパーサーを実装
- 座標はGoogleマップ埋め込みiframeの`pb`パラメータから直接取得（ジオコーディング不要）
- 結果: 132件、例祭率53.8%、座標132/132

### 熊本（scrape_kumamoto.py 新規）
- カスタム投稿タイプ`patron-god`の一覧はREST APIでlink/titleのみ取得可（571件）
- 住所はACFがREST非公開のため個別ページHTML（`table.c-table-single`）から取得が必要
  → 571ページの個別リクエストが発生（実行に数分）
- 例祭・御祭神・座標は一切掲載なし。香川方式で名称+住所のみ、GSIジオコーディングで座標付与

### 鹿児島（scrape_kagoshima.py 新規）
- 通常post（`/wp-json/wp/v2/posts`）で全1250件取得、本文に「神社名：」を含む1104件が対象
- `<div id="detail"><ul><li>ラベル：値</li>...` 形式で神社名/カナ/鎮座地/例祭日/通称/旧社格/神紋/摂末社/社宝が並ぶ
- 御祭神は別途`<h4>御祭神</h4>`直後の`div.templeMain ul.gList`から取得
- 由緒は著作権配慮で取得しない
- 例祭日は**漢数字**（例: 十一月十三日、九月二十三日）。全角空白区切りで複数日程が並び、
  末尾に（春祭）等の括弧付き名前が付くことがある → トークン分割＋末尾括弧抽出でパース
- 住所は郵便番号付き県名なし → 郵便番号除去＋「鹿児島県」前置、GSIジオコーディング

## 今後の課題
- 長崎: ドメイン消失の再確認（数ヶ月後に再訪問して移転先の有無を確認）
- 高知: ヘッドレスブラウザ（Playwright等）が使えるようになれば再挑戦の価値あり
  （Wix Velo Cloud DataのAPIエンドポイントは`_api/cloud-data`と判明済み、
  gridAppId等の情報も取得済みなので足がかりにはなる）
- 大分・宮崎: 神社庁公式サイトが存在しないため、今回は対象外。将来的に非公式サイト
  （宮巡など）を使う方針転換をするか要検討
