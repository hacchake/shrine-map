# 調査メモ 2026-07-05（data.json 総合品質監査・修正フェーズ）

check_data.py（2026-07-04作成）が検出した10項目の不具合を、3段階の「波」に分けて
ユーザー確認を挟みながら修正した。総レコード数は開始時64,602件→終了時
**63,215件**（重複削除等により1,387件減）。

## 進め方の原則
- 1修正=1コミット、各修正後にcheck_data.pyを再実行して該当項目の件数を確認
- 事前調査の想定と実態が食い違った場合は、作業を止めてAskUserQuestionでユーザーに確認
  （このセッションで4回発生。詳細は各波の章を参照）

## 第1波

### ①hiroshima_jinjacho 完全重複削除（2,080件→0件、commit e34aadc）
1,040組すべてが2件ずつ完全一致（idフィールド欠落・name/address/festivals等すべて
同一）の重複と判明。各組の2件目を削除。64,602→63,562件。

### ②chiba_jinjacho ルビ連結修正（2,492件→0件、commit 89335eb）
「アイカワジンジャ相川神社」のように冒頭のカタカナ読みが漢字名と区切りなく
連結していたバグ。読み部分を除去し漢字名のみ残すよう修正。
副作用: osm×chibaで名前・住所が一致する新規重複2件が表面化（後述④で対応）。

### ③座標不整合576件の再ジオコーディング（→事後に大幅見直し、commit f203b96/6e921cc/a931a3e）
- 事前調査で576件中567件は「県境bboxの矩形判定による誤検出」（実座標は正しい
  可能性大）、本当に壊れていたのは9件のみと判明 → ユーザーに確認
- ユーザーは「576件全部を指示通り実行」を選択 → 実行の結果、**338件（osm由来、
  住所空）が再取得不能で座標を完全に失う事故が発生**
- 事後に修正前コミットから座標を復元（337件、本当に壊れていた1件を除く）
- 復元時の除外リスト照合ミスでtochigi「日光大室高靇神社」（住所の「県」字欠落）が
  誤って壊れた座標のまま復元されてしまっていたことが判明、これも個別に修正
- nagano「阿智神社（奥宮）」も住所の「前宮　」という前置き文字列がGSIの
  ジオコーディングを狂わせていたことが判明、修正
- **教訓**: 座標null化→再ジオコーディングは、addressが空のレコードでは
  完全に不可逆（後述のcheck_data.py改善に繋がった）

### check_data.py座標判定の改善（commit 78f00f8）
上記事故を踏まえ2点改善:
- 矩形bbox判定に約20km(0.2度)のマージンを追加（県境誤検出を削減）
- addressが空のレコードは判定対象から除外し、新設の`6b_座標判定不能_住所なし`
  （21,826件、実害なし・情報用）として別集計

## 第2波

### ①祭典ズレ328件（commit c16ed7e）
**事前調査と食い違い**: 「マージ漏れ」が原因と想定していたが、実際は
scrape_okayama.py/scrape_ehime.py（2026-07-04作成の新スクレイパー）自体の
日付パーサーが「前後」「に近い日曜日」「体育の日」等の修飾語・祝日名・
干支基準日・複数日表記・旧暦プレフィックスをカバーしきれていなかったことが
原因（マージ自体は正常）。

対応方針をユーザーに確認 → 「共通モジュールparse_festivals.pyを新設し、
テストケース先書き→実装→data.jsonへの再パース適用→スクレイパーもimport」の
折衷案を採用。

- テストケース10件（test_parse_festivals.py）を先に書いてから実装
- 日付表現を「部品（月+日/祝日名/干支基準日/曜日/相対語/修飾語）」の組み合わせ
  として認識する設計。旧暦プレフィックス・複数日表記・括弧書き補足日付
  ・「名前　日付」逆順（ehime型）にも対応
- okayama・ehimeのfestivals_rawに適用: 328件→68件（okayama 237→35、
  ehime 75→17、他5ソース未対応で変化なし）
- scrape_okayama.py/scrape_ehime.pyも同モジュールをimportする形に書き換え

### ②小口バグ一括（commit b3a5608）
- 文字化け22件: 半角カタカナ・記号(U+FF61-FF9F)を全角へ正規化。「ｹ」は
  地名の「ヶ」代用慣例に合わせ「ケ」でなく「ヶ」に上書き。ibaraki 1件は
  U+FFFDで復元不能だったため公式サイト再取得で「髙田神社」と判明・修正
- name汚染86件(hotokami): 末尾の「公式」を除去
- 住所残骸29件: ibaraki 27件は**神社庁事務局自身の住所+電話番号が誤って
  格納**（鎮座地の郵便番号に「〒」記号が無いページで、スクレイパーのregexが
  郵便番号を見逃し末尾の事務局情報を誤取得）。各source_urlを再取得し
  正しい鎮座地を取得・再ジオコーディング。hotokami/ehime各1件は郵便番号の
  重複除去
- 祭典増殖57件（kanagawa 37/hotokami 11/gifu 9）: **事前調査と再度食い違い**。
  kanagawaは582件中573件、gifuは2151件全件で常にnameが空文字という、
  フラグされた件数を大幅に超える範囲の問題と判明 → ユーザーに確認。
  kanagawaは「フラグされた37件だけでなく本質的なバグ」と判断され全582件に
  parse_concatを適用（根本解消）。gifuは「サイト自体が日付のみ掲載で
  名前情報が元々存在しない」ため、フラグされた9件（同一テキストが複数
  エントリに分裂する真のバグ）のみ修正
- 結果: 1〜4は全て0件。7_祭典増殖は57→2件

### ③osmのpref誤設定73件（commit 804b23d）
実際はosm 61・hotokami 9・nagano_jinjacho 3の計73件。addressの県名を正として
pref修正。addressも空でprefが座標と食い違うケースは0件で該当なし。

### ④osm×chiba新規重複2件（commit b798fa8）
②のルビ連結修正で表面化した「渡海神社」の重複。chiba_jinjacho側にfestivalsが
入っていたためchiba側を正としosm側を削除。

## 第2波残り＋第3波

### 祭典ズレ残り67件へparse_festivals適用（commit 0966885）
- saitama(9)・hotokami(3)・osm(2)は安全に適用、53件まで改善
- **shizuokaで重大な食い違いを発見**: scrape_shizuoka.pyの設計上、
  **全1,354件でfestivals_rawが常に空文字**（`<h6>`タグ単位で直接festivals
  配列を構築する方式のため、生テキストを保持しない）。parse_festivalsを
  適用すると全1,354件のfestivalsが空リストで消失する重大リスクがあり、
  ユーザーに確認 → フラグされた1件（天宮神社の未分離1エントリ）のみ
  手動でname/date_str分離
- 再発防止ガード追加（commit f08a4e7）: check_data.pyのcheck_festival_dup・
  check_festival_misalignはfestivals_rawが空のレコードを検査対象外に。
  parse_festivals.pyに`reparse_record(d, parser)`ヘルパーを新設し、
  空rawなら何もせずFalseを返す設計に

### 祭典増殖の残り2件（commit b6883f6）
kanagawaの「七五三」2件は「本来の日付」と「近い日曜日への振替実施日」を
同名で別々に掲載する正当なデータと判明（分裂バグではない）。
check_festival_dupを「nameが完全一致する組み合わせは分裂疑いの対象外」に
調整し誤検知を解消。7_祭典増殖は2件→0件。

### 完全重複779件（commit 9f2fc71）
**事前調査と最大の食い違い**: 「osm×神社庁」の重複が779件全部だと想定していたが、
実際は**6件(3組)のみ**で、残り773件は同一source内の重複だった。
内訳をユーザーに提示して確認:

| 分類 | 件数 | 内容 | 対応 |
|---|---|---|---|
| 真の重複バグ | 310組 | 同一神社が2つのURLから重複取得（shiga 286組・akita 12組・toyama(同source内)7組・shimane 2組・aomori_jinjacho_list 2組・nara 1組） | マージ後に片方を削除 |
| 誤検知 | 17組 | kumamoto(15組): 住所が市区町村名までしかなく同名の別の実在神社が多数（例:「西岡神宮」だけで30件）。yamagata(2組): 同住所だが祭神・例祭日が異なる別レコード（合祀等） | 対応除外（削除禁止） |
| 本来のosm/jinjajin×神社庁重複 | 3組6件 | toyama×osm 2組、shizuoka×jinjajin 1組 | 神社庁側を正に、非公式側の情報（jinjajin側の例祭16件等）をマージしてから削除 |

ユーザーは全カテゴリ対応を選択。779件→119件（kumamoto 115・yamagata 4の
誤検知分は対応除外）。63,561→63,215件。

### 形式不備7,447件の正規化（commit 7c0317f）
- idフィールド欠落7,114件（nara/shimane/shiga/akita/hiroshima/mie/miyagi/
  tochigi/ibarakiの各jinjacho、いずれも元々id自体を持たない設計）に
  `{source}_{5桁連番}`形式で機械付与。付与後に全63,215件のid一意性を確認
- festivals=null 2,556件を[]に正規化
- month範囲外は0件（該当なし）
- 7,447件→0件

## 最終マトリクス（総レコード数 63,215件）

| 項目 | 結果 |
|---|---|
| 1_文字化け | 0 |
| 2_name汚染 | 0 |
| 3_ルビ連結 | 0 |
| 4_住所残骸 | 0 |
| 5a_県名プレフィックスなし（低優先） | 9,629（既知の許容事項・下記） |
| 5b_県名不一致 | 0 |
| 6_座標不整合 | 0 |
| 6b_座標判定不能・住所なし | 21,826（既知の許容事項・下記） |
| 7_祭典増殖 | 0 |
| 8_祭典ズレ | 53（既知の許容事項・下記） |
| 9_形式不備 | 0 |
| 10_完全重複 | 119（既知の許容事項・下記） |

### 既知の許容事項（今回対応せず、理由付きで容認）

- **5a_県名プレフィックスなし 9,629件**: addressが都道府県名から始まらない
  だけの低優先事項。hyogo/nagano/shiga/hiroshima/tottori等、元々スクレイパーが
  県名を前置しない方針で収集したデータが大半で、実害（地図表示や検索）は無い
- **6b_座標判定不能・住所なし 21,826件**: addressが空で座標の妥当性を
  検証する手がかりが無いレコード（大半はosm 21,538件）。誤りと判定しても
  修正しようがなく、2026-07-04〜05の座標null化事故を踏まえて意図的に
  「判定不能」として分離した集計。実害なし
- **8_祭典ズレ 53件**: okayama 35・ehime 17・osm 1。汎用パーサー
  (parse_festivals.py)で対応しきれない情報源側の表記ゆれ（「2016/12/01」の
  埋め込み等の明らかなサイト側データ不備、「現在は」等の自然文条件節）や、
  osmの1件は「例祭：紀元祭」という名前に正当な「：」を含む誤検知
- **10_完全重複 119件**: kumamoto_jinjacho 115件（住所が市区町村名までしか
  無く同名の別の実在神社が多数）・yamagata_jinjacho 4件（同住所で祭神・
  例祭日が異なる別レコード、合祀等）。いずれも削除すると実在データを
  失うため意図的に対応除外

## 教訓

- **座標のnull化→再ジオコーディングは、addressが空のレコードでは不可逆**。
  事前に「address有無」の内訳を確認してから実行すること
- **festivals_rawを保存しない設計のスクレイパー（<h6>タグ単位で直接構築等）
  には再パース処理を絶対に適用してはいけない**。festivals_rawが空文字の
  レコードは`reparse_record()`ヘルパー（parse_festivals.py）を必ず経由し、
  直接`d['festivals'] = parser(...)`と書かないこと
- **check_data.pyの「完全重複」「県名不一致」等の機械的チェックは、住所の
  粒度が粗いsource（市区町村名までしかない等）では誤検知しやすい**。
  件数の内訳（同一source内か cross-source か、内容が完全一致かどうか）を
  必ず確認してから一括処理すること
- Windows端末のコンソール（cp932）にUTF-8文字列を直接printすると文字化けする
  （実データは正常）。ファイル書き出し→Readツールで確認するか
  `PYTHONIOENCODING=utf-8`を使うこと（過去のNOTESにも記載済みだが今回も
  複数回踏んだため再掲）

## 追記: 高知県神社庁データ追加（品質監査フェーズ完了後、同日中に対応）

`https://www.kochi-jinjyacho.com/高知県神社一覧` は過去のセッション
（NOTES_20260704.md）で「Wixサイトの神社一覧がJSレンダリングのため
ヘッドレスブラウザ無しでは取得不可」と判断していたが、検索エンジンには
神社名・住所がテキストで見えていた点をユーザーが指摘、再調査した。

### 突破口
一覧ページのリピーターコンポーネント自体はクライアントサイドでWix Cloud
Dataを取得して描画する作りで、生HTMLに神社データは含まれない。しかし
各ページのHTMLには`<script type="application/json" id="wix-warmup-data">`
としてSEO/ハイドレーション用のJSONが埋め込まれており、その中の
`appsWarmupData.dataBinding.dataStore.recordsByCollectionId`に**そのページ分
（12件）のレコードがそのまま入っている**ことを発見。ページ送りパラメータ
`?comp-ljqfjyd6_page=N`を変えて再取得するたびに中身が該当ページの12件に
変わることを確認し、通常のGETリクエスト（ブラウザ不要）で全件取得できると
判明した。総件数も同JSON内の`datasetSize.total`（4,215件）から取得可能。

### スキーマ
フィールドのdisplayNameから: title=支部, text=神社名, text1=住所（市郡より
下位）, text2=電話, text3=宮司名, text4=郵便番号, newField=県市郡（住所の
上位部分）, url=URL, image=画像。例祭・御祭神のフィールド自体が存在せず
香川方式（名前+住所のみ）。電話・宮司名は個人情報配慮でスキップ。

### 実装・実行（scrape_kochi.py新規作成）
- 352ページ（12件×352≒4,215件）を1秒間隔で取得。1ページ（page=116）で
  まれにwarmup-data自体が欠落する応答が発生（thunderbolt側の一時的な
  描画ゆれと推定）→ リトライ機構を追加して解消
- **情報源側の重複を発見**: 取得した4,214件中4,210件が2,105組の完全一致
  ペアだった。収集元のWixコレクション自体に同じ神社が異なる`_id`で2件ずつ
  登録されている（協会側のCMS入力ミスと推定）。`_id`でのdedupだけでは
  検出できないため、scrape_kochi.pyに(name, address)完全一致での重複除去
  ステップを追加。真の件数は2,109件
- name中の半角カタカナ（例:「須ﾉ浦神社」）も他県と同様の慣例で全角に正規化
- 座標はGSI APIでジオコーディング（2,109件中2,106件成功）
- merge_new_prefectures.pyでマージ: 65,324件

### 結果
data.json: 63,215件 → **65,324件**（高知2,109件追加）。check_data.py全項目
クリア（マージ直後に1_文字化け22件・10_完全重複4,210件が新規発生したが、
上記の正規化・重複除去をscrape_kochi.py側に組み込んだ上で再マージし解消）。

CLAUDE.mdの未完了タスク13番「高知はWixサイトでJSレンダリングのため取得不可」
の判断を撤回し完了に更新。

## 次フェーズ候補（UI改善、未着手）

- famous_manual（v1〜v4計22社）の星マーカー表示
- 詳細モーダルの仕上げ（実装済み・ユーザーのブラウザ確認待ちの状態から進める）
- デフォルト現在地起動（初期表示の自動現在地取得は実装済みとのSUMMARY記載あり、
  要再確認）
