# 作業結果まとめ（2026-07-03）

自動実行フェーズ（岩手・山形・青森・香川・沖縄）の結果まとめ。

## 県別結果

| 県 | 件数 | 例祭率 | ステータス | 備考 |
|---|---|---|---|---|
| 岩手 | 857件 | 97.4% (835/857) | マージ・push済み | scrape_iwate.pyのバグ2件を修正 |
| 山形 | 1,729件 | 99.0% (1,712/1,729) | マージ・push済み | エラーなし |
| 青森 | 732件（詳細98＋一覧のみ634） | 13.0% (95/732) | マージ・push済み | 一覧のみ神社を追加抽出（下記） |
| 香川 | 723件 | 0.0%（仕様通り・例祭フィールドなし） | マージ・push済み | scrape_kagawa.pyのバグ1件を修正 |
| 沖縄 | 16件 | 6.2% (1/16) | ジオコーディング・マージ・push済み | 手動キュレーションデータ |

## data.json 合計件数の推移

52,738件 → 53,595件（岩手）→ 55,324件（山形）→ 55,422件（青森・初回98件）→ 56,145件（香川）→ 56,161件（沖縄）
→ 56,795件（青森拡張、98件→732件に置換）→ **56,795件**（全国ジオコーディングは既存レコードの座標補完のみで件数変化なし）

## 第2フェーズ（青森拡張・全国ジオコーディング・奈良調査）

### 青森: 詳細ページなし神社の追加抽出（98件→732件）
- 一覧ページのテーブル行は[番号,名前,住所]の3セル形式。名前セルに`<a>`が無い行＝個別詳細ページの無い神社と判明
- `get_list_only_shrines()`を追加し、名前＋住所のみ（例祭・御祭神は空）で634件を追加抽出、source=`aomori_jinjacho_list`
- 全732件ジオコーディング成功。軽微な残存: 完全一致の重複が2組4件（実害小さく未対応）

### 全国ジオコーディング強化
- `geocode_file.py data.json --data-format` で対象3,408件中3,348件（98.2%）を補完
- 残217件: 157件はaddress自体が空（新規データ収集が必要）、60件はaddressありだがGSI解決失敗（千葉・長野・岩手・京都・静岡など複数県に分散、単一原因なし）
- 静岡の一部レコードは神社名・住所が文字化けしていることを発見（scrape_shizuoka.py側の別問題、今回は範囲外）
- 副作用: geocode_file.pyのjson保存整形(indent=1)がmerge_new_prefectures.py(コンパクト)と異なり、コミット差分が全行分に膨張。実害なし

### 奈良の例祭パース率（1.2%）調査 → スクレイパーのバグではなくサイト側データの疎さが原因、対応スキップ
- ランダム30件を直接フェッチ確認 → 29/30件で「年中行事」欄が完全に空。唯一データがあった1件も具体的な日付の記載なし
- 奈良県神社庁サイト自体が例祭日をほとんど公開しておらず、スクレイパー改善では解決不可。代替ソースが必要（優先度低）

## 発見・修正したバグ

### scrape_iwate.py
1. 郵便番号除去の正規表現が半角数字前提だったが、サイトは全角数字（０１９等）を使用 → src全体を半角化して解決
2. `grab()`のキャプチャが無制限で、meta descriptionが「由緒」ラベル手前で途切れる記事だと本文のTEL/FAX/サイドバーまで巻き込んでいた → キャプチャ長を200文字に制限、「連絡先」もストップラベルに追加
3. 郵便番号の区切り文字が全角ハイフン「－」(U+FF0D)で正規表現の文字クラスに未対応だった → 追加
- 残存問題: 郵便番号の残骸が3件（0.35%）。いずれもサイト側の表記ゆれが原因で実害は小さい

### scrape_kagawa.py
- 初回実行で御祭神・鎮座地とも全件空（0/723）という重大な不具合を発見
- 原因: ラベルと「：値」が別行にレンダリングされるページ構造に対し、正規表現が同一行の空白しか許容していなかった
- 単純に改行許容にすると、値が空欄のフィールドで次のラベル文字列自体を値として誤取得する副作用が発生（例: 御祭神が空欄なのに"URL"を誤取得）
- ラベル→コロン間（改行可）とコロン→値間（同一行のみ）で異なる空白パターンを使う修正で解決
- 修正後: 御祭神141/723件、座標722/723件取得に回復

## 未解決・要フォローアップ

- ~~青森のカバレッジ~~ → 対応済み（98件→732件）
- ~~奈良の例祭パース率改善~~ → 調査済み・サイト側データ疎につき対応不可と判断（スキップ）
- ~~ジオコーディング全体強化~~ → 対応済み（98.2%補完）。残217件のうち60件はaddressありでも解決不可、うち静岡5件はscrape_shizuoka.py側の文字化けが原因の可能性（未着手）
- UI改善（詳細モーダル、カタカナ表示修正）（未着手）
- data.jsonのjson整形をmerge_new_prefectures.pyとgeocode_file.pyで統一する（未着手、優先度低）

## 作業中に得た教訓（NOTESにも記録済み）

- Windows環境でPythonの標準出力を直接表示すると文字化けする場合がある → `PYTHONIOENCODING=utf-8`を付けて実行すれば解決（一時ファイル経由は不要）
- ラベル:値形式のスクレイパーでは「ラベル→区切り文字」と「区切り文字→値」で改行許容を分けて設計すること。両方とも`\s*`にすると、値が空欄のフィールドで次のラベルを誤取得するバグを踏みやすい（香川で実例）

## 県別集計（2026-07-04時点、data.json 56,795件・47都道府県）

### 件数ワースト10（少ない順）
| 県 | 件数 | 例祭率 |
|---|---|---|
| 高知県 | 6件 | 0.0% |
| 佐賀県 | 36件 | 0.0% |
| 沖縄県 | 77件 | 1.3% |
| 大分県 | 88件 | 1.1% |
| 山梨県 | 123件 | 0.8% |
| 長崎県 | 134件 | 0.0% |
| 熊本県 | 140件 | 0.0% |
| 富山県 | 152件 | 0.0% |
| 鹿児島県 | 167件 | 0.0% |
| 宮崎県 | 173件 | 1.7% |

### 例祭率ワースト10（低い順）
| 県 | 件数 | 例祭率 | 備考 |
|---|---|---|---|
| 福島県 | 434件 | 0.0% | |
| 新潟県 | 321件 | 0.0% | |
| 富山県 | 152件 | 0.0% | 全件osm由来、jinjacho未取得 |
| 福井県 | 581件 | 0.0% | 全件osm由来、jinjacho未取得 |
| 徳島県 | 609件 | 0.0% | |
| 香川県 | 807件 | 0.0% | サイトに例祭フィールド自体が無い（仕様通り） |
| 群馬県 | 1,248件 | 0.0% | |
| 高知県 | 6件 | 0.0% | |
| 佐賀県 | 36件 | 0.0% | |
| 長崎県 | 134件 | 0.0% | |

富山・福井は現状すべてosm（OpenStreetMap）由来で神社庁データが未反映であることを確認 → 次フェーズで対応
