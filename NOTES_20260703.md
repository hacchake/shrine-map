# 調査メモ 2026-07-03（東北4県＋富山 神社庁サイト検証）

## 結論サマリ

| 県 | ソース | 判定 | 期待件数 | 例祭日 |
|---|---|---|---|---|
| 岩手 | 神道青年会 livedoorブログ | ◎ 新規スクレイパー作成済 | 約857社 | あり（郵便番号付住所も） |
| 山形 | yamagata-jinjyacho.or.jp | ◎ スクレイパー修正済 | 1,729社 | あり（例祭日欄。ただし未記載の社もある） |
| 青森 | aomori-jinjacho.or.jp | ◎ スクレイパー修正済 | 8地区分 | あり（例祭欄） |
| 北海道 | hokkaidojinjacho.jp | △ 未検証 | 不明 | サイト説明上はあり |
| 富山 | toyama-jinjacho.sakura.ne.jp | × 低優先 | - | なし（第三者評: 住所・電話のみ） |

## 各県詳細

### 岩手（scrape_iwate.py 新規）
- 神社庁公式 jinjacho.jp は2025年末リニューアルで神社検索が**廃止**（旧 serviceindex1.html → 404確認）
- 代替: 神社庁トップからリンクされる **岩手県神道青年会** 作成DB https://ganshinsei.livedoor.blog/
- 1記事=1神社、市町村カテゴリ合計 約857社（一関117・花巻111・奥州79・北上65・盛岡61…）
- 記事構造: 神社名/御祭神/**例祭日**/鎮座地（〒付き）/由緒 — meta descriptionに全フィールド入りでパース容易
- 由緒（長文）は転載を避け取得しない。事実データのみ
- source名は `iwate_ganshinsei`（神社庁ではないため）
- URL収集: sitemap.xml → 失敗時 ?p=N（1記事/ページ×858）

### 山形（scrape_yamagata.py 修正）
- `/shrine_search` が全1,729社を12件×145ページで列挙（旧コードの /shrine_list 等は存在せず）
- ページャは `?random_key=XXX&page=N`。ページ内の次リンクを辿る方式に修正（random_key継承）
- 詳細ページ: 鎮座地/御祭神/例祭日（dl形式想定＋div形式フォールバック追加）
- 座標抽出を生HTML対象に修正（get_text()はhref属性を含まないため旧コードは実質不動作）
- GSIジオコーディング追加

### 青森（scrape_aomori.py 修正）
- リスト sub_1{a-h}.html と個別ページのURL構造は旧コードの想定通り生存
- **重大バグ修正**: 実ページはテーブル形式でラベルが「住 所」「例 祭」「由 緒」と*スペース入り*。
  旧コードは `line == '住所'` の完全一致だったため**例祭率0%になるところだった**
- テーブル直接パース＋ラベル空白正規化＋行ベースフォールバックに書き換え
- 県名プレフィックス判定も「青森」→「青森県」に修正（青森市問題）。岩手（岩手郡）・山形（山形市）も同様に修正
- GSIジオコーディング追加

### 北海道（scrape_hokkaido.py 未検証）
- hokkaidojinjacho.jp/jinja/ に支部別DB（例祭日等掲載と明記）はあるが、
  **robots.txtがクローラーを拒否**しており外部検証不可だった
- 実行するかは判断が必要（低頻度・公益目的とはいえサイト側は自動アクセス非許容のシグナル）
- 代替: 札幌支部は独自サイト jinjasapporo.net に /find-shrine/{神社名}/ 形式で掲載

### 富山（未着手・低優先）
- toyama-jinjacho.sakura.ne.jp に「富山県氏神神社検索」あり
- ただし第三者レビュー（alpharigid）曰く「由来等の詳細データは無く、住所や電話番号を検索する機能しかありません」
- **例祭日なしの可能性大** → 福井と同カテゴリ。マップの主目的には寄与しないため後回し

## 実行手順（Termux）

```bash
cd ~/shrine-map && git pull

# 所要時間目安（sleep込み）
python3 scrape_iwate.py      # 約15-20分（URL収集+857記事+ジオコーディング）
python3 scrape_yamagata.py   # 約20-25分（145ページ+1729詳細+ジオコーディング）
python3 scrape_aomori.py     # 約10分（8地区）

# マージ（--dry-run で件数確認してから）
python3 merge_new_prefectures.py iwate_raw.json yamagata_raw.json aomori_raw.json --dry-run
python3 merge_new_prefectures.py iwate_raw.json yamagata_raw.json aomori_raw.json

git add -A && git commit -m "Add iwate/yamagata/aomori data" && git push
```

## パーサー検証状況
- 3スクレイパーとも、実サイトからフェッチしたページ構造を再現した合成HTMLで単体テスト済み（2026-07-03）
- 実サイトへの通し実行は未実施（Termuxで要実行）
- 万一実ページで0件/低率になる場合は、まず1ページだけ curl して構造差分を確認すること

---

# 追記（同日・優先順位2: 香川・沖縄）

## 香川（scrape_kagawa.py 新規）
- kagawakenjinjacho.or.jp はWordPress。/municipalities/{slug}/ に13市町村の一覧、詳細は /shrine/{URLエンコード名-N}/
- 詳細フィールドは「神社コード/神社名/鎮座地/電話/FAX/御祭神/URL/特記事項」で**例祭フィールドが存在しない**（実ページ確認済み）
- → festivalsは常に空。県内約730社の住所+御祭神+座標のカバレッジ目的（現状7件→約730件）
- ページネーションは /page/N/ を新規リンク0件まで追従（存在しなければ1ページで停止）

## 沖縄（okinawa_raw.json 手動キュレーション）
- 神社庁包括はわずか**10社**（神社庁トップ jinjacho.naminouegu.jp に全社掲載）→ スクレイパーは過剰と判断
- 包括10社＋護国神社・沖宮・出雲大社沖縄分社・護佐喜御宮・生天光神明宮・大東神社の計16件を作成
- 例祭日確認済みは大東神社(9/23)のみ。琉球八社は各公式サイトで個別確認可能（notesにURL記載）
- 浮島/世持/沖縄神社は神社庁記載が社務所住所（波上宮内）のため、鎮座地ベースの住所に置換し精度をnotesに明記

## geocode_file.py（新規・汎用）
- 任意のraw json（またはdata.json本体 --data-format）の座標なしレコードをGSIで補完
- 優先順位5（ジオコーディング強化）にもそのまま使える
- 沖縄: `python3 geocode_file.py okinawa_raw.json` → merge

## ⚠ GitHub PAT無効
- 2026-07-03時点でPATがAPI 401（無効/失効/revoke）。プッシュ不可のためコミットはローカルのみ
- 新PATはfine-grained（対象リポジトリ限定・Contents: Read and write のみ）を推奨
- **→ 追記(同日夜)**: ブラウザ経由のgit-credential-manager認証でpush可能になった。以降のpushは全て成功（詳細は下記）

---

# 追記（同日夜・実行フェーズ: 岩手・山形・青森・香川・沖縄）

## 結果サマリ

| 県 | 件数 | 例祭率 | 備考 |
|---|---|---|---|
| 岩手 | 857件 | 97.4% | scrape_iwate.pyのバグ2件を修正（下記） |
| 山形 | 1,729件 | 99.0% | エラーなし。想定件数と一致 |
| 青森 | 98件 | 96.9% | **カバレッジ課題あり**（下記） |
| 香川 | 723件 | 0.0%（仕様通り） | scrape_kagawa.pyのバグ1件を修正（下記） |
| 沖縄 | 16件 | 6.2% | geocode_file.pyで全件ジオコーディング成功 |

**data.json 合計: 52,738件 → 56,161件**（+3,423件）

## scrape_iwate.py 修正内容
1. 郵便番号除去の正規表現が半角`\d`前提だったが、サイトは全角数字（０１９等）→ src全体を半角化して解決
2. `grab()`が無制限マッチ(`(.+?)`)で、meta descriptionが「由緒」ラベル手前で途切れる記事だと本文のTEL/FAX/サイドバーまで巻き込んでいた → キャプチャ長を200文字に制限＋「連絡先」もストップラベルに追加
3. 郵便番号の区切りが全角ハイフン「－」(U+FF0D)で、正規表現の文字クラス`[-−ーｰ]`に入っていなかった → 追加
- 残存: 郵便番号3件のみ残存（0.35%）。いずれもサイト側の表記ゆれ（〒の位置が逆・郵便番号3桁のみ・ハイフン欠落）。実害小さいため未対応

## scrape_kagawa.py 修正内容
- 初回実行で御祭神・鎮座地とも**全件空**（0/723）という重大な不具合が発覚
- 原因: `FIELD_RE`のラベル→コロン間の空白パターンが行内空白のみ(`[ \t　]*`)だったが、実ページは`soup.get_text('\n')`で「鎮座地\n ： 値」のようにラベルとコロンが**別行**にレンダリングされる。改行を跨げず全フィールドがマッチ失敗していた
- 単純に`\s*`に変えると別の不具合発生: 値が空欄のフィールド（例: 御祭神が未記載）で「：」直後の複数改行を`\s*`が飛び越え、**次のラベル文字列自体を値として誤取得**していた（例: 御祭神が空欄なのに"URL"を値として拾う）
- 最終修正: ラベル→コロン間は`\s*`（改行可）、コロン→値は`[ \t　]*`（同一行のみ）の2種類に分離
- 修正後: 御祭神141/723件、座標722/723件取得

## 青森のカバレッジ課題（未解決・要判断）
- scrape_aomori.pyは正常動作（エラーなし、住所品質も問題なし）だが、取得できたのは**98件のみ**
- 一覧ページ（sub_1a〜h.html、8地区）自体に、個別詳細ページへのリンクを持たない「神社名＋住所のみ」の行が多数存在することを確認（例: 地区bだけで100件超のテーブル行、リンクは20件のみ）
- 全8地区でページネーション（続きページ）は無し（確認済み）→ 見落としではなく、サイト構造上「詳細ページがある神社」と「一覧に住所だけ載っている神社」が混在
- 香川と同様に「リンクなし神社の名前＋住所のみ抽出」を追加すれば大幅に件数を増やせる可能性がある（青森県の神社庁包括社数は本来もっと多いはず）
- 今回は時間の都合上98件のまま確定・マージ。**次回セッションでの優先候補**: 全地区の一覧ページから住所直接抽出（香川方式）を追加実装

## 沖縄
- geocode_file.pyで16件全件ジオコーディング成功（宮古島・南大東島含め座標は妥当な範囲）
- そのままマージ。例祭確認済みは引き続き大東神社(9/23)のみ

## 作業中に得た教訓
- Windows環境でPythonの標準出力をリダイレクト/直接表示するとcp932とUTF-8の不一致で文字化けする。`PYTHONIOENCODING=utf-8`を付けて実行すれば直接表示できる（一時ファイル経由は不要）

---

# 追記（翌夜・青森拡張／全国ジオコーディング／奈良調査）

## 青森: 詳細ページなし神社の追加抽出 → 98件→732件
- scrape_aomori.pyに`get_list_only_shrines()`を追加。一覧ページ（sub_1a〜h.html）の全テーブル行は[番号,名前,住所]の3セル形式で統一されており、名前セルに`<a>`が無い行＝個別詳細ページの無い神社
- これらを名前＋住所のみ（例祭・御祭神は空）で抽出、source を`aomori_jinjacho_list`として区別
- merge_new_prefectures.pyは新規データの先頭レコードのsourceでのみ既存データ削除対象を判定するため、順序（詳細ページ分を先に、リスト限定分を後に追加）に注意。今回は`aomori_jinjacho`が常に先頭に来るため既存98件は正しく置換された
- 結果: 732件（詳細ページ98件＋名前住所のみ634件）、全件ジオコーディング成功、例祭率13.0%（分母が増えたため岩手/山形より低いが想定通り）
- 軽微な残存: 完全一致の重複が2組4件（サイト側のテーブルに同一神社が2回掲載されている可能性）。実害小さいため未対応

## 全国ジオコーディング強化（geocode_file.py data.json --data-format）
- 対象3,408件中3,348件（98.2%）を補完
- 残り217件: 157件はaddress自体が空（ジオコーディングでは解決不可、新規データ収集が必要）。60件はaddressありだがGSIが解決できず（千葉23・長野7・岩手7・京都6・静岡5・群馬4など複数県に分散、単一の共通原因なし）
- 静岡の一部（5件）は神社名・住所が文字化けした状態でdata.jsonに入っている（例:「貊昴ヮ螳ｮ逾樒､ｾ」）。scrape_shizuoka.py側のエンコーディング処理に問題がある可能性。今回は範囲外としてスキップ
- **副作用**: geocode_file.pyは`json.dump(..., indent=1)`で保存するため、merge_new_prefectures.py（`separators=(',', ':')`でコンパクト保存）で保存された既存data.jsonと整形が異なり、diffが全行分（93万行超）に膨れた。実害はないがコミット差分が読みにくい。次回はどちらかの整形設定に統一するとよい

## 奈良の例祭パース率（1.2%）を調査 → スクレイパーのバグではなくサイト側データの疎さが原因、対応スキップ
- ランダム30件の実ページを直接フェッチして確認 → 29/30件で「年中行事」欄が完全に空
- 唯一データがあった1件（八咫烏神社）も「歳旦祭・祈年祭・夏越の大祓・例祭・新嘗祭・年越の大祓」と行事名の列挙のみで、例祭の具体的な日付は記載なし
- つまり奈良県神社庁サイト（narashinsei.com）自体が例祭日をほとんど公開していない。scrape_nara.pyの正規表現やロジックを改善しても抽出できるデータが元から存在しないため、修正を見送り
- 改善する場合は代替ソース（各神社の公式サイト等）が必要だが、コスト対効果が低いため優先度は下げてよい
- ラベル:値形式のスクレイパーで「ラベル→区切り文字」と「区切り文字→値」は改行許容を分けて考えること。前者は改行を跨ぐ場合があるが、後者を`\s*`にすると値が空欄のフィールドで次のラベルを誤取得する（香川で実例）

---

# 追記（翌々日・県別集計／富山・福井）

## 県別集計（data.json 56,795件時点、47都道府県）
- 件数・例祭率ワースト10を算出しSUMMARY.mdに追記
- 富山県(152件)・福井県(581件)がいずれもosm（OpenStreetMap）由来のみで神社庁データ未反映と判明 → 次の対応対象に選定

## 富山県神社庁「氏神神社検索」(toyama-jinjacho.sakura.ne.jp) → scrape_toyama.py新規作成
- robots.txtは`/main/wp-admin/`のみ拒否。クロール自体は許可されている
- サイト本体（トップ）の検索ページはiframeで別WPサブサイト`/ujigami/`を埋め込んでいる
- `/ujigami/`はWP REST API(`wp-json/wp/v2/posts`)が検索クエリに対して空を返す（投稿タイプが除外されているか別経由のため）→ 使用不可
- 代わりにWP標準検索(`?s=市町村名`)とページネーション(`/page/N/?s=...`)でURL収集。15市町村（富山市など）を全部検索
- 個別記事はtable形式（神社名/社名カナ/郵便番号/住所/宮司名/電話番号/FAX番号/鎮座地/御神札/ホームページ）
- **御祭神フィールドがサイトに存在しない**（第三者レビュー通りの制約を実地確認）→ deityは常に空。宮司名・電話・FAXは個人/連絡先情報のため取得せず
- 結果: 3,188件、住所品質良好（残骸0件、県名プレフィックス正常）、全件ジオコーディング成功。完全一致重複7組（実害小さく未対応）

## 福井県神社庁「神社を探す」(www.jinja-fukui.jp) → scrape_fukui.py新規作成
- 検索: 富井は https://www.jinja-fukui.jp/ が正しい公式ドメイン（fukui-jinjacho.jp等は存在しない）
- robots.txtは`/maint/`のみ拒否。クロール許可
- 市町村検索はJSでフォーム送信されるが実体はPOST `/result/`（SearchType=1, Chiku=市町村コード01-17）。1リクエストで該当市町村の全神社が返る（ページネーション無し、確認済み）
- 検索結果に名前・住所は既に含まれる。**御祭神は詳細ページ（GET /detail/index.php?ID=xxx）にのみ存在**するため、1,695件全件を個別に追加フェッチ
- 「主な祭典」欄は例祭情報を含みうるが、ほぼ空かつ非構造的なため取得対象外（ユーザー指示通り例祭は範囲外）
- 結果: 1,695件、御祭神率99.8%、住所品質良好、全件ジオコーディング成功

## data.json 最終件数
56,795件 → 58,490件（福井）→ **61,678件**（富山）
